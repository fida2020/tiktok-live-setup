This folder held a local design-reference video (`background-animation-reference.mp4`, ~10MB)
used while designing the animated background loops. It's excluded from this repo as a video
file. Supply your own reference clip here if you need it — nothing in `src/` reads from this
folder at runtime.
